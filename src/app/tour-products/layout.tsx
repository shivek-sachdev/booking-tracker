import React from 'react';

// Basic layout for the tour products section
export default function TourProductsLayout({ children }: { children: React.ReactNode }) {
  return (
    <>{children}</> // Simple pass-through, can be customized later if needed
  );
} 