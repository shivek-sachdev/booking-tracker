import React from 'react';

// Basic layout for the tour packages section
export default function TourPackagesLayout({ children }: { children: React.ReactNode }) {
  return (
    <>{children}</> // Simple pass-through, can be customized later if needed
  );
} 